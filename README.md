# AI Image Generator for WordPress

Генерируйте уникальные изображения для постов с помощью искусственного интеллекта через Puter.js SDK.

## 🚀 Основные возможности

- **Автоматическая генерация** изображений на основе содержимого поста
- **Несколько AI-моделей**: GPT Image 1, DALL-E 3, GPT Image 1 Mini
- **Настройка качества**: Low (быстро), Medium, High (лучшее качество)
- **Автопромпты**: Плагин сам создает промпты из текста статьи
- **Ручная настройка**: Редактирование промптов, выбор стилей
- **Интеграция с медиабиблиотекой**: Изображения сохраняются в WordPress
- **Пакетная обработка**: Генерация для нескольких постов одновременно
- **История генераций**: Сохранение всех созданных изображений

## 📋 Требования

- WordPress 5.0 или выше
- PHP 7.2 или выше
- Современный браузер с поддержкой JavaScript ES6+

## 🔧 Установка

1. Скачайте ZIP-архив плагина
2. В админ-панели WordPress: Плагины → Добавить новый → Загрузить плагин
3. Выберите архив и установите
4. Активируйте плагин
5. **Никаких API ключей не требуется!**

## 💡 Использование

### Генерация изображения для поста:

1. Создайте или отредактируйте пост
2. В правой боковой панели найдите блок "Generate AI Image"
3. При необходимости отредактируйте промпт
4. Выберите модель и качество
5. Нажмите "Generate Image" (1-3 минуты)
6. Используйте результат как миниатюру записи

### Настройки плагина:

1. Перейдите в Настройки → AI Image Generator
2. Выберите модель по умолчанию
3. Настройте качество изображений
4. Включите автогенерацию при сохранении (опционально)

## 💰 Модель оплаты (User-Pays)

Плагин использует модель **User-Pays** от Puter.js:

- **Для владельца сайта**: плагин бесплатен
- **Для авторов/редакторов**: каждый платит за свои генерации
- **Примерные цены**: $0.005-$0.04 за изображение
- **Пополнение**: через аккаунт Puter (есть бесплатный тариф для теста)

**Преимущества**:

- Нет скрытых платежей
- Нет месячных подписок
- Каждый контролирует свои расходы
- Масштабируемость без увеличения стоимости

## 🔐 Безопасность и конфиденциальность

- Контент поста передается только как текстовый промпт
- Никакие файлы не загружаются на сторонние серверы
- Изображения хранятся на вашем хостинге
- Нет доступа к базе данных WordPress

## 🛠 Для разработчиков

### Фильтры:

```php
// Изменение промпта перед генерацией
add_filter('aigfp_prompt', function($prompt, $post_id) {
    return $prompt . " In minimalist flat design style.";
}, 10, 2);

// Добавление своих стилей изображений
add_filter('aigfp_image_styles', function($styles) {
    $styles['custom'] = 'My custom style description';
    return $styles;
});

```

### Действия:

```php

// После генерации изображения
add_action('aigfp_image_generated', function($post_id, $attachment_id, $prompt) {
    // Логирование или дополнительные действия
}, 10, 3);

```

## 📊 Статистика

Плагин собирает анонимную статистику:

- Количество сгенерированных изображений
- Используемые модели
- Среднее время генерации
- Популярные стили

## 🆘 Поддержка

- Документация: [developer.puter.com](https://docs.puter.com/)
- Сообщество: WordPress Support Forum
- Баг-репорты: GitHub Issues

## 📄 Лицензия

GPL v2 или выше

## 🙏 Благодарности

- Puter.js SDK за AI инфраструктуру
- Сообществу WordPress за вдохновение
- Всем тестировщикам и контрибьюторам

**Сделано с ❤️ для сообщества WordPress**
